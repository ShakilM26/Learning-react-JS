import {z} from 'zod';

export const skillSchema = z.object({
    userName: z.string().min(3, 'Name must be 2 letters'),
    skills: z.array(z.object({
        skillName: z.string().min(1, 'cannot be left blank')
    })).min(1, 'add at least 1 skill')
})
